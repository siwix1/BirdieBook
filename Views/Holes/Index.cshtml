@model IEnumerable<BirdieBook.Models.Hole>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
                <th>
                    @Html.DisplayNameFor(model => model.HoleNumber)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Par)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Length)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.HCPIndex)
                </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.HoleNumber)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Par)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Length)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.HCPIndex)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.HoleID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.HoleID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.HoleID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
